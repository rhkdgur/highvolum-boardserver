<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.highvolume.boardserver.mapper.UserMapper">

    <select id="getUser" resultType="com.highvolume.boardserver.dto.UserDTO">
        SELECT id, userId, password, nickName, createTime, isWithDraw, status
        FROM user
        WHERE id = #{id}
    </select>

    <insert id="insertUser" parameterType="com.highvolume.boardserver.dto.UserDTO">
        INSERT INTO user (userId, password, nickName, isWithDraw, status, isAdmin)
        VALUES (#{userId}, #{password}, #{nickName}, #{isWithDraw}, #{status}, #{isAdmin})
    </insert>

    <update id="updateUser" parameterType="com.highvolume.boardserver.dto.UserDTO">
        UPDATE user
        SET password=#{password},
            nickName=#{nickName},
            isWithDraw=#{isWithDraw},
            status=#{status}
        WHERE id = #{id}
    </update>

    <delete id="deleteUser">
        DELETE
        FROM user
        WHERE userId = #{id}
    </delete>

    <insert id="register" parameterType="com.highvolume.boardserver.dto.UserDTO">
        INSERT INTO user (userId, password, nickName, createTime, isWithDraw, status)
        VALUES (#{userId}, #{password}, #{nickName}, #{createTime}, #{isWithDraw}, #{status})
    </insert>

    <select id="findByIdAndPassword" resultType="com.highvolume.boardserver.dto.UserDTO">
        SELECT id,
               userId,
               password,
               nickName,
               createTime,
               isWithDraw,
               status
        FROM user
        WHERE id = #{id}
          AND passWord = #{password}
          AND status != 'DELETE'
    </select>

    <select id="findByUserIdAndPassword" resultType="com.highvolume.boardserver.dto.UserDTO">
        SELECT id,
               userId,
               password,
               nickName,
               createTime,
               isWithDraw,
               status
        FROM user
        WHERE userId = #{userId}
          AND password = #{password}
          AND status != 'DELETE'
    </select>

    <select id="idCheck" resultType="int">
        SELECT COUNT(id)
        FROM user
        WHERE userId = #{userId}
    </select>

    <update id="updatePassword" parameterType="com.highvolume.boardserver.dto.UserDTO">
        UPDATE user
        SET password = #{password}
        WHERE userId = #{userId}
    </update>

</mapper>